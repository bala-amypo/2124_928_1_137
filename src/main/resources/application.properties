spring.application.name=sql

# -------------------------------
# DO NOT CHANGE (as requested)
# -------------------------------
server.port=9001
server.forward-headers-strategy=framework

# -------------------------------
# DATABASE (H2 – IN MEMORY)
# -------------------------------
spring.datasource.url=jdbc:h2:mem:user_role;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=org.h2.Driver

# -------------------------------
# JPA / HIBERNATE
# -------------------------------
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# -------------------------------
# H2 CONSOLE (OPTIONAL BUT USEFUL)
# -------------------------------
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# -------------------------------
# JWT (keep if already used)
# -------------------------------
jwt.secret=secretKey123456
jwt.expiration=3600000
